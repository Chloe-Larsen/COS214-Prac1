# Project, Yayy

(this needs to be redone lol)

## Makefile Commands

- `make` - build project to `engine` file
- `make run` - build project and run
- `make zip` - create submission zip file including all `.cpp` and `.h` files, `Makefile`, and `answers.pdf`
- `make clean` - remove build artifacts